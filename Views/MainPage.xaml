<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SailMonitor.MainPage">
    
    
    <VerticalStackLayout x:Name="MainLayout" Padding="10" Spacing="10" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
        <VerticalStackLayout.GestureRecognizers>

            <SwipeGestureRecognizer Direction="Left"
                                    Swiped="OnSwipeLeft" />

            <SwipeGestureRecognizer Direction="Right"
                                    Swiped="OnSwipeRight" />
        </VerticalStackLayout.GestureRecognizers>
        <!-- Navigation buttons -->
        <HorizontalStackLayout x:Name="ButtonHzStack" HorizontalOptions="Center" Spacing="20">
            <Button x:Name="PrevButton" Text="◀ Prev" Clicked="Prev_Clicked" />
            <Button x:Name="NextButton" Text="Next ▶" Clicked="Next_Clicked" />
        </HorizontalStackLayout>


        <ContentView x:Name="content"
             HorizontalOptions="FillAndExpand"
             VerticalOptions="FillAndExpand" >

            <ContentView.GestureRecognizers>
                <SwipeGestureRecognizer Direction="Left"
                     Swiped="OnSwipeLeft" />
                <SwipeGestureRecognizer Direction="Right"
                     Swiped="OnSwipeRight" />
            </ContentView.GestureRecognizers>
        </ContentView>





    </VerticalStackLayout>

</ContentPage>
